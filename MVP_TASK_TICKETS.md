# MVP Task Tickets

이 문서는 [AGENTS.md](AGENTS.md) 기준을 실제 구현 단위로 분해한 실행 티켓입니다.

## Ticket 1 - Ready 상태 동기화 (최우선)
- 목표: 로비에서 각 플레이어의 Ready 상태를 네트워크로 동기화하고, Host만 게임 시작 가능하도록 보장.
- 범위:
  - 플레이어별 Ready 토글 데이터(NetworkVariable 또는 동등한 NGO 동기화 구조) 추가.
  - Host는 모든 필수 조건 충족 시에만 Start 버튼 활성화.
  - 늦게 들어온 클라이언트도 현재 Ready 상태를 즉시 수신.
- 완료 조건:
  - 4인 기준에서 Ready/Unready 변경이 모든 클라이언트에 1초 내 반영.
  - Host가 아닌 클라이언트는 Start를 실행할 수 없음.
  - 로비 재입장 시 상태 초기화 규칙이 명확히 적용됨.
- 회귀 점검:
  - Main Menu -> Lobby -> Game Map 전환 정상.
  - Host 종료/이탈 시 클라이언트가 안전하게 복귀 처리.

## Ticket 2 - 파밍 상호작용 네트워크 동기화
- 목표: 월드 상호작용(줍기/놓기/소유권 변경)에서 중복 획득, 유령 아이템, 권한 충돌 방지.
- 범위:
  - 상호작용 판정은 Host 권한으로 처리.
  - 상호작용 결과를 RPC/NetworkObject 상태로 전파.
  - 동일 오브젝트 동시 상호작용 경쟁 상황 처리(선점/거절 응답).
- 완료 조건:
  - 2명 이상이 같은 아이템을 동시에 상호작용해도 결과가 단일하게 결정.
  - 클라이언트 재접속 후 아이템 상태 불일치 없음.
  - 아이템 소유자/월드 상태가 모든 클라이언트에서 일관됨.
- 회귀 점검:
  - 4인 동시 접속 중 상호작용 폭주 테스트 통과.

## Ticket 3 - 격자 인벤토리 네트워크 모델 1차
- 목표: 타르코프식 격자 인벤토리의 서버 기준 데이터 모델을 정의하고 동기화.
- 범위:
  - 인벤토리 데이터 구조(슬롯 좌표, 크기, 회전, 아이템 ID, 수량) 설계.
  - 클라이언트 요청(이동/회전/분할) -> Host 검증 -> 확정 반영 흐름 구현.
  - 충돌 검사(겹침, 범위 초과) 서버 검증 필수.
- 완료 조건:
  - 잘못된 배치 요청이 Host에서 거절되고 클라이언트에 사유 반환.
  - 정상 배치 요청은 모든 참여자에게 동일하게 반영.
  - 저장/복구 포인트(세션 단위 직렬화 가능 구조) 준비.
- 회귀 점검:
  - 인벤토리 조작 중 씬 전환/재접속 시 데이터 일관성 유지.

## Ticket 4 - 세션 라이프사이클 안정화 (Host/Join/Leave/Reconnect)
- 목표: Relay 조인코드 기반 세션 생명주기에서 실패 케이스를 표준화.
- 범위:
  - 실패 코드/상태 메시지 표준화(잘못된 코드, 만료, 풀방, 연결 끊김).
  - 재접속 처리 정책(자동/수동 재시도) 정리 및 구현.
  - 이벤트 누수 방지(등록/해제, 씬 전환 시 중복 핸들러 차단).
- 완료 조건:
  - 주요 실패 시나리오에서 UI가 멈추지 않고 재시도 가능 상태 유지.
  - 동일 세션에서 Leave 후 재Join 시 크래시/중복 플레이어 없음.
  - 로그에서 세션 이벤트 순서가 예측 가능하게 유지.
- 회귀 점검:
  - 첫 접속/재접속/Host 종료 경로 모두 수동 테스트 완료.

## Ticket 5 - 회귀 자동 점검 체크리스트 + 실행 스크립트 정리
- 목표: 필수 회귀 검증을 반복 가능한 절차로 고정.
- 범위:
  - 테스트 체크리스트 문서화(씬 전환, 4인 스폰, Host 이탈, 재접속).
  - 개발 중 빠르게 돌릴 수 있는 테스트 순서(수동 + 가능한 자동화) 정의.
  - 릴리즈 전 최소 검증 게이트 정의.
- 완료 조건:
  - 팀원이 문서만 보고 동일한 회귀 점검을 재현 가능.
  - 최소 1회 이상 전체 흐름 검증 기록 남김.
  - 치명 이슈 발생 시 롤백/핫픽스 기준 명시.
- 회귀 점검:
  - 본 티켓 자체가 회귀 체계 확립을 목적으로 하므로 전 티켓 공통 게이트로 사용.

---

## 권장 작업 순서
1. Ticket 1 완료 후 Ticket 4 일부(이벤트 안정화) 병행.
2. Ticket 2와 Ticket 3은 Host 검증 패턴을 공유하도록 공통 유틸 먼저 정리.
3. Ticket 5를 마지막에 몰지 말고 각 티켓 완료 시 체크리스트 항목을 즉시 갱신.

## Definition of Done (공통)
- NGO + UGS Multiplayer 방향을 유지하고 standalone Lobby/Relay 혼용 없음.
- PlayerInput 오너 입력 원칙 위반 없음.
- Main Menu -> Lobby -> Game Map 흐름 회귀 없음.
- 4인 접속/스폰 시나리오에서 치명 오류 없음.
